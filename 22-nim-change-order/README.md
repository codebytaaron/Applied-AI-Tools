# nim-change-order

Nim CLI template that drafts a change order document.

This repo is intentionally written in **Nim** (not Python/JS) so GitHub shows a different language mix.

## Run
```bash
nim c -r main.nim
```

## Input format
Put your notes in `input.txt` (or pipe stdin). Output is printed to stdout.
