# shell-daily-brief

Shell script template that formats a daily crew brief.

This repo is intentionally written in **Shell** (not Python/JS) so GitHub shows a different language mix.

## Run
```bash
bash main.sh
```

## Input format
Put your notes in `input.txt` (or pipe stdin). Output is printed to stdout.
