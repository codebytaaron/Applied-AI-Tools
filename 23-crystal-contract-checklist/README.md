# crystal-contract-checklist

Crystal CLI template that outputs a contract review checklist.

This repo is intentionally written in **Crystal** (not Python/JS) so GitHub shows a different language mix.

## Run
```bash
crystal run main.cr
```

## Input format
Put your notes in `input.txt` (or pipe stdin). Output is printed to stdout.
