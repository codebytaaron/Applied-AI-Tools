# julia-forecast-notes

Julia CLI template that summarizes forecast assumptions and actions.

This repo is intentionally written in **Julia** (not Python/JS) so GitHub shows a different language mix.

## Run
```bash
julia main.jl
```

## Input format
Put your notes in `input.txt` (or pipe stdin). Output is printed to stdout.
