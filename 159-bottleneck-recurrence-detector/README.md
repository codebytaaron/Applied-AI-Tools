# Bottleneck Recurrence Detector

Automatically analyzes and structures insights for bottleneck recurrence detector.

## Run
```bash
pip install -r requirements.txt
cp .env.example .env
python main.py
```
